DB_URL=jdbc:postgresql://localhost:5432/leather_db
DB_USERNAME=leather
DB_PASSWORD=leather_dev_pass
# Neon / Render (preferred in cloud):
# DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require

JWT_SECRET=replace-with-a-long-random-secret-at-least-32-characters
# Must be set on Render. Example strong secret:
# openssl rand -base64 48
JWT_EXPIRATION_MS=86400000

FILE_STORAGE_PATH=./uploads
FILE_STORAGE_URL=/uploads

# Local + GitHub Pages origin (required for free Pages ↔ Render integration)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://itsumeet27.github.io

SERVER_PORT=8080
SEED_DATA=true
JPA_DDL_AUTO=update
PUBLIC_API_BASE_URL=
